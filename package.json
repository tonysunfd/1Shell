{
  "name": "1shell",
  "version": "4.1.0",
  "description": "1Shell - AI-assisted VPS and server management console",
  "main": "desktop/main.js",
  "scripts": {
    "start": "bash scripts/start-bootstrap.sh",
    "dev": "nodemon server.js",
    "desktop:prepare": "npm --prefix frontend run build",
    "desktop:dev": "electron .",
    "desktop:build": "electron-builder",
    "desktop:build:dir": "electron-builder --dir",
    "desktop:build:win": "electron-builder --win",
    "desktop:build:linux": "electron-builder --linux",
    "desktop:build:mac": "electron-builder --mac",
    "build:agent": "node scripts/build-agent.js",
    "package:macos": "node scripts/package-macos-release.js",
    "test": "node scripts/test-risk-rules.js && node scripts/test-file-service.js"
  },
  "keywords": [
    "ssh",
    "web-terminal",
    "devops",
    "ai",
    "mcp",
    "server-management",
    "agentless"
  ],
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "better-sqlite3": "^11.7.0",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "helmet": "^8.1.0",
    "iconv-lite": "^0.7.2",
    "js-yaml": "^4.1.1",
    "multer": "^2.1.1",
    "node-cron": "^4.2.1",
    "node-fetch": "^2.7.0",
    "node-pty": "^1.1.0",
    "socket.io": "^4.7.5",
    "ssh2": "^1.17.0"
  },
  "devDependencies": {
    "electron": "^33.4.11",
    "electron-builder": "^25.1.8",
    "nodemon": "^3.1.4",
    "socket.io-client": "^4.8.3"
  },
  "build": {
    "appId": "com.oneshell.app",
    "productName": "1Shell",
    "asar": false,
    "npmRebuild": false,
    "nodeGypRebuild": false,
    "directories": {
      "output": "release/desktop"
    },
    "files": [
      "desktop/**/*",
      "server.js",
      "src/**/*",
      "lib/**/*",
      "bin/**/*",
      "agent/**/*",
      "public/**/*",
      "frontend/public/favicon.png",
      "frontend/dist/**/*",
      "data/skills/**/*",
      "runtime/node/**/*",
      ".env.example",
      "install.sh",
      "LICENSE",
      "README.md",
      "package.json"
    ],
    "win": {
      "artifactName": "1shell-desktop-${version}-windows-${arch}.${ext}",
      "target": [
        "nsis",
        "zip"
      ]
    },
    "nsis": {
      "artifactName": "1shell-desktop-${version}-windows-${arch}-setup.${ext}",
      "oneClick": false,
      "perMachine": false,
      "allowToChangeInstallationDirectory": true
    },
    "mac": {
      "artifactName": "1shell-desktop-${version}-macos-${arch}.${ext}",
      "target": [
        "dmg"
      ],
      "category": "public.app-category.utilities"
    },
    "linux": {
      "artifactName": "1shell-desktop-${version}-linux-x64.${ext}",
      "target": [
        "AppImage"
      ],
      "category": "Utility"
    }
  }
}
